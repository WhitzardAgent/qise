"""CLI entry point for Qise.

Provides the ``qise`` command with subcommands:
    qise proxy start   — start the API proxy server
    qise proxy takeover — auto-configure an agent to use the proxy
"""

from __future__ import annotations


def main() -> None:
    """CLI entry point (placeholder)."""
    print("Qise — AI-first runtime security framework for AI agents")
    print("Usage: qise proxy start [--port PORT] [--config CONFIG]")


if __name__ == "__main__":
    main()
